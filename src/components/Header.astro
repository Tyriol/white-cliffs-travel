<header>
		<p class="site-title">White Cliffs Travel</p>
        <button popovertarget="site-nav-popover">
            <svg viewBox="0 0 100 80" width="40" height="40">
                <rect width="100" height="15"></rect>
                <rect y="30" width="100" height="15"></rect>
                <rect y="60" width="100" height="15"></rect>
            </svg>
        </button>
		<nav popover="auto" id="site-nav-popover">
            <button class="demo-button" popovertarget="site-nav-popover" popovertargetaction="hide">
		        Close
            </button>
            <ul class="header-nav">
				<li class="nav-link">Deals</li>
				<li class="nav-link">Blog</li>
				<li><button class="contact-btn">Book your consultation</button></li>				
			</ul>
		</nav>
	</header> 

    <style>
        header {
            height: 6rem;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            /* box-shadow:5px 5px 10px black; */
	    }   
        
        button {
            border: none;
        }

        /* .site-title {
            font-size: 1.5rem;
            font-weight: 600;
        } */

        .header-nav {
            list-style: none;
            padding-left: 0;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: center;
            height: 2.5rem;
            gap: 1rem;
        }

        #site-nav-popover[popover] {
            transition:
                display 0.3s allow-discrete,
                overlay 0.5s allow-discrete,
                opacity 0.3s,
                translate 0.3s;
            transition-timing-function: ease-in;
            translate: 100%;
            margin: 0;
            block-size: 100vb;
            inline-size: 100vi;
            inset-inline-start: unset;
            inset-inline-end: 0;

            &:popover-open {
                translate: 0;
                transition-timing-function: ease-out;
            }

            @starting-style {
                &:popover-open {
                    translate: 100%;
                }
            }
    	}      

        .nav-link {
            transition: all 0.25s;
            height: 2.5rem;
            width: 4rem;
            font-weight: 500;
            align-content: center;
            text-align: center;

        .nav-link:hover {
            transition: all 0.25s;
            box-shadow:2px 2px 5px black;
            border-radius: 0.5rem;
        }

        /* .header-nav > li:last-of-type {
            width: auto;
        } */
    </style>